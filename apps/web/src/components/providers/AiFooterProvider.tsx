'use client';

import { AiFooterBanner } from '../legal/AiFooterBanner';

/**
 * Client wrapper to render the AI footer banner in the server layout.
 */
export function AiFooterProvider() {
  return <AiFooterBanner />;
}
